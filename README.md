# 🐍 Aprendizagem Python

Repositório criado para registrar meu progresso nos estudos de Python. Aqui você encontra exercícios e exemplos práticos organizados por tema.

---

## 📁 Estrutura dos Arquivos

| Arquivo | Descrição |
|---|---|
| `hello.py` | Primeiro programa — saída de texto com `print()` |
| `tipos_de_dados.py` | Tipos primitivos: int, float, bool e string |
| `variaveis_constantes.py` | Declaração de variáveis, atribuição múltipla e constantes |
| `conversao_de_tipos.py` | Conversão entre tipos com `int()`, `float()` e `str()` |
| `funcao_saida_entrada.py` | Entrada de dados com `input()` e saída com `print()` |
| `concatenacao.py` | Concatenação de strings com `+` e f-strings |

---

## 📚 Conteúdo por Tema

### Hello World
Primeiro contato com Python, usando a função `print()` para exibir uma mensagem na tela.

### Tipos de Dados
Exploração dos tipos primitivos do Python:
- `int` — números inteiros
- `float` — números decimais
- `bool` — valores `True` e `False`
- `str` — texto (strings)

### Variáveis e Constantes
Como declarar variáveis, reatribuir valores e realizar atribuição múltipla em uma única linha. Convenção de nomes para constantes em `SNAKE_CASE_MAIÚSCULO`.

### Conversão de Tipos
Uso das funções de conversão para transformar dados entre diferentes tipos, como converter string para número ou número para string.

### Entrada e Saída de Dados
Uso das funções `input()` para capturar dados do usuário e `print()` para exibir resultados formatados. Inclui o uso de `type()` para verificar o tipo de uma variável.

### Concatenação de Strings
Duas formas de combinar strings e variáveis:
- Usando o operador `+`
- Usando **f-strings** (forma moderna e recomendada)

---

## 🛠️ Como Executar

Você precisa ter o Python 3 instalado. Para rodar qualquer arquivo:

```bash
python nome_do_arquivo.py
```

Exemplo:
```bash
python hello.py
```

---

## 📈 Progresso

- [x] Hello World
- [x] Tipos de dados
- [x] Variáveis e constantes
- [x] Conversão de tipos
- [x] Entrada e saída de dados
- [x] Concatenação de strings
- [ ] Condicionais (`if`, `elif`, `else`)
- [ ] Laços de repetição (`for`, `while`)
- [ ] Funções
- [ ] Listas e dicionários

---

*Estudando Python do zero* 🚀
